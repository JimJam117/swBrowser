import React, { Component } from 'react';

export class ships extends Component {
    render() {
        return (
            <div>
                ships
            </div>
        );
    }
}

export default ships;
